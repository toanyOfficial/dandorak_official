{
  "scripts": {
    "build": "echo build-complete",
    "start": "npm run images:menu-thumbs || echo \"menu thumbnail generation skipped\"; bun run server.js",
    "dev": "npm run images:menu-thumbs || echo \"menu thumbnail generation skipped\"; bun run server.js",
    "db:health": "bun run scripts/db-health.js",
    "menu:query": "bun run scripts/menu-query.js",
    "images:menu-thumbs": "python3 scripts/generate-menu-thumbnails.py"
  },
  "dependencies": {
    "dotenv": "^16",
    "express": "^4",
    "mysql2": "^3"
  }
}
